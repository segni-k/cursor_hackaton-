# Admin controllers

Place admin HTTP controllers here, e.g. `DashboardController`, `UserController`.

Register routes in `routes/admin.php` (included by `routes/web.php`).

**User** (patients & pharmacy owners) lives under `App\Http\Controllers\User\`. **Landing** is `App\Http\Controllers\Landing\`. Keep admin-only logic here.
