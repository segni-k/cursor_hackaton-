# Landing (marketing)

- `LandingController` serves the public homepage.
- Routes live in `routes/landing.php` (required early from `routes/web.php`).
- Frontend: `resources/js/Pages/Landing/` and `resources/js/Components/Landing/`.

This is intentionally separate from **Admin**, **Patient**, and **Pharmacy** areas.
